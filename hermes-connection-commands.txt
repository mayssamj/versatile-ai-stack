openshell sandbox exec -n hermes-fleet-v1 --tty -- hermes --profile <p> -m
  local -z "…"

hermes --profile hermes_cos --version

openshell sandbox exec -n hermes-fleet-v1 --tty --

# sandbox: deny first by default linux container
# -n hermes-fleet-v1 (--name hermes-fleet-v1) --> which sandbox? the one hosting hermes-fleet-v1
# --tty --> allocate a pseudo tty for interactive use
# -- - ends openshell's own options: everything after it is the command + args run inside the sandbox
# without it openshell would try to parse hermes's flags (-m -z) as its own . it's the standard argv separator

hermes --profile <p> -m local -z "..."
# hermes: /sandbox/.venv/bin/hermes v0.15.2
# --profile <p> --> load sould.md , config and sessions for p. --> vs hermes profile use <p> --> set a sticky default. 
# valid profile values: default, hermes_cos, hermes_creator, hermes_data_analyst, hermes_ops, hermes_researcher, hermes_reviewer, hermes_software_engineer

# -m local or --model: ... model override for this invocation (applies to -z/--tui); routes through the
  profile's LiteLLM provider. Also settable via HERMES_INFERENCE_MODEL. (All profiles currently show
  local because LM Studio is down → availability-gated.)

# -z "…" — --oneshot PROMPT: one-shot mode — send the single prompt "…" and print only the final response
  text to stdout (no banner, spinner, tool-previews, or session-id line). Tools/memory/rules/AGENTS.md in the
  CWD are still loaded; approvals are auto-bypassed. Built for scripts/pipes. The next token is the prompt
  verbatim (a leading-- prompt errors rather than being read as a flag).

#  Related flag the bridge adds: --yolo — bypass all dangerous-command approval prompts (needed for
  headless/--no-tty runs).



openshell sandbox exec -n hermes-fleet-v1 --tty -- hermes --profile <p> -m local -z "…"

# --> souls in openshell/fleet-souls/*.md; .hermes at /sandbox/.hermes (hermes-fleet-v1).

