The one-liners (this is the whole thing now)

# Metered (works immediately — your OPENAI_API_KEY is set):
vz-ai-stack.sh model assign all openai-gpt-5.5          # whole fleet → GPT-5.5, max reasoning (xhigh)
vz-ai-stack.sh model assign hermes_manager openai-gpt-5.5-pro

# Subscription (no metered cost — your bridge is already enabled):
vz-ai-stack.sh model assign all openai-gpt-5.5-sub      # whole fleet → GPT-5.5 on your ChatGPT plan
# (if the bridge ever needs re-enabling: bash ~/ai-stack/bin/start-codex-bridge.sh enable — one command)

model assign accepts these valid GPT ids now: openai-gpt-5.5, openai-gpt-5.5-pro, openai-gpt-5.4 (metered) and openai-gpt-5.5-sub, openai-gpt-5.4-sub (subscription). Max thinking (reasoning_effort: xhigh) is baked into 5.5. Short how-to: doc/GPT5.md (also in models.md, TUTORIAL L6).

What made it seamless (the engine change)

I taught the model-binding engine two new runtimes — openai (metered) and codex-bridge (your subscription) — so GPT-5.x is now first-class assignable (it was config-only/unassignable before). model assign registers the model, widens the scoped keys, and re-points the agent in one shot. A missing key or down bridge gates to local with a visible pending line — never a hard fail, never a surprise metered bill.

Verified live from main (not just merged)

- HERMES scoped key now covers all five GPT ids → fleet-assignable proven.
- openai-gpt-5.5 chat via that scoped key → GPT5-XHIGH-OK (max-reasoning config loaded, didn't break).
- openai-gpt-5.5-sub → SUB-OK end-to-end (your bridge is live).
- 2nd model sync = UNCHANGED (idempotent, no needless restarts); hermetic smoke 9/9.

§24: 3-agent design council + 2-agent implementation review → SHIP; every must-fix applied (two availability gates, integer rpm/tpm, literal key sentinel, idempotency). Shipped + pushed (dec8cae).

Two honest flags

1. Your push now includes a parallel session's bare-hostname-ingress feature — it was sitting on your local main (unpushed, flagged in-progress in my notes), so pushing my work carried it to origin/main too. It's your own work and council-reviewed, but if you didn't want it public yet, say so and I'll help unwind it.
2. Metered GPT spends your real OpenAI bill — there's no per-key budget cap yet (I deferred that as a tracked follow-up; it's bounded by a soft rate guard + your own OpenAI limits). For cost-conscious fleet use, the -sub (subscription) models are the answer — they draw on your ChatGPT plan, not your card. Effort note: xhigh is accepted on the subscription route but its honoring is unobservable; the metered route is the verifiable max-reasoning one.


