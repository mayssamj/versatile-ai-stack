{
  "name": "@ai-stack/honcho-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP server exposing Honcho memory (remember/recall/ask/search) over stdio and HTTP, for cross-runtime consumption (host Claude Code/Pi + Hermes fleet). The token-gated HTTP transport is the only sandbox path to Honcho once the raw :8000 egress is retired.",
  "bin": {
    "honcho-mcp": "bin.mjs"
  },
  "scripts": {
    "start:stdio": "node bin.mjs --stdio",
    "start:http": "node bin.mjs --http",
    "test": "node test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
